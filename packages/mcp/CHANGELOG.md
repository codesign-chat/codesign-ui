# @codesign-ui/mcp

## [1.0.0] - 2026-09-16

### Added

- Initial stable release of `@codesign-ui/mcp` under the `@codesign-ui` scope, providing documentation search
  (`search_docs`) and retrieval (`get_docs`) tools for MCP clients. Earlier version numbers predate the Codesign UI
  rebrand and were never published.
